services:
  signaling:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./signaling:/app
    command: ["sh", "-c", "npm install --omit=dev && node server.js"]
    environment:
      PORT: "8080"
      HOST: "0.0.0.0"
    restart: unless-stopped
    ports:
      - "8080:8080"
